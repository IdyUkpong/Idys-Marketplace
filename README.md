# E-Commerce
 A basic Express full satck application, that makes a CRUD operation (create, read, update, delete) using  json file, document and publish your endpoints using postman.
In this project, you’ll build a basic CRUD (Create, Read, Update, Delete) for an Ecommerce Application, using MongoDB as database.
